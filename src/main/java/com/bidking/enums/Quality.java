package com.bidking.enums;

/**
 * 物品品质等级（由低到高）
 * WHITE(白色) → GREEN(绿色) → BLUE(蓝色) → PURPLE(紫色) → GOLD(金黄色) → RED(红色)
 */
public enum Quality {
    WHITE,   // 白色
    GREEN,   // 绿色
    BLUE,    // 蓝色
    PURPLE,  // 紫色
    GOLD,    // 金黄色
    RED      // 红色
}
